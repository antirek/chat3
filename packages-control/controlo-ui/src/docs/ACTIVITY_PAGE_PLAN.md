# План реализации вкладки "Активность"

## Цель
Добавить новую вкладку "Активность" в controlo-ui под HealthCheck с отображением двух графиков:
1. **Events & Updates** - столбчатый график за последние 30 дней (два столбика каждый день)
2. **API запросы** - столбчатый график за последние 30 дней по ApiJournal

## Технические решения

### Библиотека для графиков
- **chart.js** + **vue-chartjs** (легковесная, хорошо интегрируется с Vue 3)

### Фильтрация данных
- Агрегированные данные по **всем тенантам вместе** (без фильтрации по tenantId)

### Формат дат
- Формат отображения: **"год-месяц-день"** (YYYY-MM-DD), например "2024-01-16"

### Период данных
- **Фиксированный период**: последние 30 дней (без возможности выбора периода)

### Обновление данных
- **Без автообновления**: данные загружаются только при загрузке страницы

---

## Этап 1: Backend (controlo-backend)

### 1.1. Создать контроллер `activityController.ts`

**Путь:** `packages/controlo-backend/src/controllers/activityController.ts`

**Методы:**

#### `getEventsUpdatesStats(req, res)`
- **Endpoint:** `GET /api/activity/events-updates`
- **Описание:** Получает статистику Events и Updates за последние 30 дней
- **Логика:**
  1. Вычисляем начальную дату (30 дней назад от текущего момента)
  2. MongoDB агрегация:
     - Группировка Events по дням (из `createdAt`)
     - Группировка Updates по дням (из `createdAt`)
     - Объединение результатов
  3. Заполнение пропущенных дней нулями
  4. Сортировка по дате
- **Формат ответа:**
  ```json
  {
    "data": {
      "dates": ["2024-01-01", "2024-01-02", ...],
      "events": [10, 15, 20, ...],
      "updates": [5, 8, 12, ...]
    }
  }
  ```

#### `getApiRequestsStats(req, res)`
- **Endpoint:** `GET /api/activity/api-requests`
- **Описание:** Получает статистику API запросов из ApiJournal за последние 30 дней
- **Логика:**
  1. Вычисляем начальную дату (30 дней назад от текущего момента)
  2. MongoDB агрегация ApiJournal:
     - Группировка по дням (из `createdAt`)
     - Подсчет количества запросов на день
  3. Заполнение пропущенных дней нулями
  4. Сортировка по дате
- **Формат ответа:**
  ```json
  {
    "data": {
      "dates": ["2024-01-01", "2024-01-02", ...],
      "requests": [100, 150, 200, ...]
    }
  }
  ```

### 1.2. Создать роуты `activityRoutes.ts`

**Путь:** `packages/controlo-backend/src/routes/activityRoutes.ts`

**Структура:**
```typescript
import express from 'express';
import activityController from '../controllers/activityController.js';

const router = express.Router();

router.get('/events-updates', activityController.getEventsUpdatesStats);
router.get('/api-requests', activityController.getApiRequestsStats);

export default router;
```

### 1.3. Подключить роуты в `index.ts`

**Путь:** `packages/controlo-backend/src/index.ts`

**Изменения:**
- Импортировать `activityRoutes`
- Добавить `app.use('/api/activity', activityRoutes);`

### 1.4. Добавить Swagger документацию

**В `activityController.ts` добавить JSDoc комментарии:**
```typescript
/**
 * @swagger
 * /api/activity/events-updates:
 *   get:
 *     summary: Get Events and Updates statistics for last 30 days
 *     tags: [Activity]
 *     ...
 */
```

---

## Этап 2: Frontend (controlo-ui)

### 2.1. Установить зависимости

**Команда:**
```bash
cd packages/controlo-ui
npm install chart.js vue-chartjs
```

**Зависимости:**
- `chart.js` - библиотека для графиков
- `vue-chartjs` - обертка для Vue 3

### 2.2. Создать структуру страницы

**Директория:** `packages/controlo-ui/src/pages/activity/`

**Файлы:**
```
activity/
├── index.ts                          # Экспорт страницы
├── ui/
│   ├── ActivityPage.vue             # Основной компонент страницы
│   └── charts/
│       ├── EventsUpdatesChart.vue   # График Events & Updates
│       └── ApiRequestsChart.vue     # График API запросов
└── model/
    └── useActivityStats.ts          # Composable для загрузки данных
```

### 2.3. Реализация компонентов

#### `ActivityPage.vue`
- Контейнер для двух графиков
- Заголовок страницы "Активность"
- Использование `useActivityStats` для загрузки данных
- Обработка состояний загрузки и ошибок

#### `EventsUpdatesChart.vue`
- Компонент графика на основе `vue-chartjs`
- Тип: `bar` (столбчатый)
- Два набора данных: Events и Updates
- Цвета: разные для Events и Updates
- Ось X: даты в формате YYYY-MM-DD
- Ось Y: количество

#### `ApiRequestsChart.vue`
- Компонент графика на основе `vue-chartjs`
- Тип: `bar` (столбчатый)
- Один набор данных: API запросы
- Ось X: даты в формате YYYY-MM-DD
- Ось Y: количество запросов

#### `useActivityStats.ts`
- Composable для работы с API
- Методы:
  - `loadEventsUpdatesStats()` - загрузка данных Events & Updates
  - `loadApiRequestsStats()` - загрузка данных API запросов
- Использование `getControlApiUrl` для формирования URL
- Обработка ошибок
- Состояния загрузки

### 2.4. Добавить роут

**Файл:** `packages/controlo-ui/src/app/router/index.ts`

**Добавить:**
```typescript
{
  path: '/activity',
  name: 'activity',
  component: () => import('@/pages/activity'),
}
```

### 2.5. Добавить ссылку в навигацию

**Файл:** `packages/controlo-ui/src/widgets/app-layout/ui/AppLayout.vue`

**Добавить после ссылки на events-updates, перед HealthCheck:**
```vue
<router-link
  to="/activity"
  class="nav-link"
  title="Активность"
>
  <span class="nav-link-icon">📈</span>
</router-link>
```

---

## Этап 3: Детали реализации

### 3.1. MongoDB агрегация для Events & Updates

**Events:**
```javascript
Event.aggregate([
  {
    $match: {
      createdAt: { $gte: startTimestamp }
    }
  },
  {
    $group: {
      _id: {
        $dateToString: {
          format: "%Y-%m-%d",
          date: { $toDate: { $multiply: ["$createdAt", 1000] } }
        }
      },
      count: { $sum: 1 }
    }
  }
])
```

**Updates:**
```javascript
Update.aggregate([
  {
    $match: {
      createdAt: { $gte: startTimestamp }
    }
  },
  {
    $group: {
      _id: {
        $dateToString: {
          format: "%Y-%m-%d",
          date: { $toDate: { $multiply: ["$createdAt", 1000] } }
        }
      },
      count: { $sum: 1 }
    }
  }
])
```

**Примечание:** `createdAt` в микросекундах, нужно умножить на 1000 для миллисекунд.

### 3.2. MongoDB агрегация для ApiJournal

```javascript
ApiJournal.aggregate([
  {
    $match: {
      createdAt: { $gte: startTimestamp }
    }
  },
  {
    $group: {
      _id: {
        $dateToString: {
          format: "%Y-%m-%d",
          date: { $toDate: { $multiply: ["$createdAt", 1000] } }
        }
      },
      count: { $sum: 1 }
    }
  }
])
```

### 3.3. Заполнение пропущенных дней

После агрегации нужно:
1. Создать массив всех дат за последние 30 дней
2. Заполнить пропущенные дни нулями
3. Отсортировать по дате

**Пример функции:**
```typescript
function fillMissingDays(dates: string[], values: number[], days: number = 30): { dates: string[], values: number[] } {
  const result: { dates: string[], values: number[] } = { dates: [], values: [] };
  const today = new Date();
  const dataMap = new Map<string, number>();
  
  // Создаем map из полученных данных
  dates.forEach((date, index) => {
    dataMap.set(date, values[index]);
  });
  
  // Заполняем все дни
  for (let i = days - 1; i >= 0; i--) {
    const date = new Date(today);
    date.setDate(date.getDate() - i);
    const dateStr = date.toISOString().split('T')[0]; // YYYY-MM-DD
    result.dates.push(dateStr);
    result.values.push(dataMap.get(dateStr) || 0);
  }
  
  return result;
}
```

### 3.4. Конфигурация графиков Chart.js

**EventsUpdatesChart:**
- Тип: `bar`
- Два dataset: Events (синий) и Updates (зеленый)
- Опции:
  - `scales.x.ticks.maxRotation: 45` - поворот подписей дат
  - `responsive: true`
  - `maintainAspectRatio: false`

**ApiRequestsChart:**
- Тип: `bar`
- Один dataset: Requests (оранжевый)
- Аналогичные опции

---

## Этап 4: Тестирование

### 4.1. Backend тестирование
- Проверить корректность агрегации данных
- Проверить заполнение пропущенных дней
- Проверить формат ответа

### 4.2. Frontend тестирование
- Проверить загрузку данных
- Проверить отображение графиков
- Проверить обработку ошибок
- Проверить навигацию

---

## Порядок выполнения

1. ✅ Создать план (этот файл)
2. ⬜ Установить зависимости (chart.js, vue-chartjs)
3. ⬜ Создать backend контроллер и роуты
4. ⬜ Протестировать backend endpoints
5. ⬜ Создать frontend структуру страницы
6. ⬜ Реализовать composable для загрузки данных
7. ⬜ Реализовать компоненты графиков
8. ⬜ Добавить роут и навигацию
9. ⬜ Протестировать полный функционал

---

## Примечания

- Все данные агрегируются по всем тенантам (без фильтрации)
- Период фиксированный: последние 30 дней
- Формат дат: YYYY-MM-DD
- Обновление данных только при загрузке страницы
- Используется библиотека chart.js + vue-chartjs
