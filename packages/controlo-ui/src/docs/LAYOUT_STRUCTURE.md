# Структура Layout приложения

## Общий Layout

В `api-test.html` есть общий layout, который используется для всех страниц (кроме Healthcheck):

### Компоненты:

1. **Header (шапка)** - `widgets/app-layout/ui/AppLayout.vue`
   - Градиентный фон
   - Название приложения
   - Мета-информация (проект, версия)
   - Ссылки на OpenAPI документацию
   - Поля для API Key и Tenant ID
   - Кнопка применения credentials

2. **Sidebar (меню слева)** - в том же компоненте
   - Навигация с иконками
   - Router links для переключения между страницами
   - Активное состояние текущей страницы

3. **Content (контент)** - `<router-view />`
   - Отображает текущую страницу
   - Каждая страница имеет свой локальный header (например, "🏢 Тенанты")

## Структура файлов

```
src/
├── app/
│   ├── App.vue                    # Использует AppLayout
│   └── stores/
│       └── credentials.ts         # Store для API Key и Tenant ID
│
├── widgets/
│   └── app-layout/
│       └── ui/
│           ├── AppLayout.vue      # Общий layout (header + sidebar + content)
│           └── index.ts
│
└── pages/
    └── TenantsPage.vue            # Страница с локальным header
```

## Как это работает

1. **App.vue** рендерит `<AppLayout />`
2. **AppLayout** содержит:
   - Header с credentials
   - Sidebar с навигацией
   - `<router-view />` для контента страниц
3. **Страницы** (например, TenantsPage) отображаются внутри `<router-view />`
4. **Credentials Store** хранит API Key и Tenant ID, доступен всем страницам

## Использование credentials в страницах

```typescript
import { useCredentialsStore } from '@/app/stores/credentials';

const credentialsStore = useCredentialsStore();

// Получить API Key
const apiKey = credentialsStore.apiKey;

// Получить Tenant ID
const tenantId = credentialsStore.tenantId;

// Получить headers для запросов
const headers = credentialsStore.getHeaders();
// Возвращает: { 'X-API-Key': '...', 'X-Tenant-ID': '...' }
```

## Отличия от HTML версии

- **HTML**: Страницы загружались в iframe, credentials передавались через query params и postMessage
- **Vue**: Используется Vue Router, credentials хранятся в Pinia store, доступны всем компонентам
