# План: страница «Топики + Сообщения»

## Цель

Добавить в sidebar новый пункт **«Топики + Сообщения»** после «Диалоги + Сообщения» и перед «Сообщения».

- **Колонка 1:** заголовок, фильтр, пагинация, таблица по топикам (tenant-level).
- **Колонка 2:** при клике по топику — только просмотр сообщений этого топика (фильтр, пагинация, таблица). Кнопку «Добавить» не делаем.

**Решения:** вариант A (новый endpoint `GET /api/topics`). В таблице топиков колонки topicId, dialogId и кнопка **Инфо** для просмотра JSON топика (в модалке видно meta и остальные поля). Колонку meta в таблицу не выводим. Маршрут `/topics-messages`, иконка 📌.

---

## 1. Backend: новый endpoint `GET /api/topics`

### Назначение

Список топиков по тенанту (все диалоги), с пагинацией и фильтром. Нужен для первой колонки страницы «Топики + Сообщения».

### Спецификация

- **Метод и путь:** `GET /api/topics`
- **Auth:** существующий apiAuth, requirePermission('read').
- **Query-параметры:**
  - `page` (number, default 1)
  - `limit` (number, default 10)
  - `filter` (string) — формат как в других списках (queryParser). Поддерживаемые поля:
    - `topicId` — eq, ne, in, nin
    - `dialogId` — eq, ne, in, nin
    - `meta.<key>` — eq, ne, in и т.д. (через extractMetaFilters для entityType `topic`)
  - `sort` (string) — например `(createdAt,asc)` / `(createdAt,desc)`, при необходимости `(dialogId,asc)` и т.д.

- **Ответ:** `{ data: Topic[], pagination: { page, limit, total, pages } }`
- **Элемент Topic в data:** минимум `topicId`, `dialogId`, `meta` (объект), при необходимости `createdAt`. Чтобы во второй колонке вызывать `GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)`, в каждом топике должны быть `topicId` и `dialogId`.

### Реализация (tenant-api)

- **Роут:** зарегистрировать в app (рядом с dialogs) роут вида `/api/topics`, GET → topicController.getTenantTopics (или getTopics).
- **Контроллер (topicController):**
  - Новый метод `getTenantTopics`: извлечь tenantId из req, page/limit/filter/sort из query.
  - Построить query по коллекции Topic: `tenantId`, применить parseFilters(filter) → regularFilters (topicId, dialogId) + metaFilters для meta.*; для meta — buildMetaQuery(tenantId, 'topic', metaFilters).
  - Sort по умолчанию например `createdAt: -1`.
  - Пагинация: skip/limit, countDocuments для total.
  - Обогатить каждый топик meta через metaUtils.getEntityMeta(tenantId, 'topic', topicId).
  - Вернуть `{ data: sanitizeResponse(topicsWithMeta), pagination }`.
- **Валидация:** использовать существующий queryWithFilterSchema для query (page, limit, filter, sort). При необходимости расширить допустимые поля фильтра для topicId/dialogId в queryParser или в контроллере (если queryParser уже отдаёт их в regularFilters).

### Место в коде

- Роут: новый файл `packages/tenant-api/src/routes/topicListRoutes.ts` (или добавить GET в отдельный роутер для `/api/topics`, чтобы не путать с `/api/dialogs/:dialogId/topics`). Либо в `index` завести `app.use('/api/topics', topicListRoutes)`.
- Контроллер: новый метод в `topicController.ts` — `getTenantTopics`.

---

## 2. Сообщения по топику (уже есть)

Запрос для второй колонки:  
`GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)&page=1&limit=10`  
Формат фильтра и пагинации уже поддерживается в messageController. Дополнительных изменений в API не требуется.

---

## 3. Frontend: маршрут и sidebar

- **Роутер:** добавить маршрут:
  - `path: '/topics-messages'`, `name: 'topics-messages'`, `component: () => import('@/pages/topics-messages')`.
- **Sidebar (AppLayout.vue):** после пункта «Диалоги + Сообщения», перед «Сообщения»:
  - `router-link` to="/topics-messages", title="Топики + Сообщения", иконка 📌.

---

## 4. Frontend: страница и модель

### Структура страницы (2 колонки)

- **Левая панель (BasePanel ~50%):**
  - Заголовок: «Топики».
  - Кнопка «URL» в header (показать URL запроса к `GET /api/topics`).
  - Фильтр топиков: поле filter (topicId, dialogId, meta.*) + примеры/пресеты по необходимости.
  - Пагинация (ExtendedPagination или аналог).
  - Таблица топиков: колонки topicId, dialogId, действия — кнопка **Инфо** (открывает модалку с полным JSON топика, там видно meta). Клик по строке = выбор топика (сохраняем `topicId` и `dialogId` из выбранного элемента ответа API).

- **Правая панель (BasePanel ~50%):**
  - Заголовок: «Сообщения».
  - Если топик не выбран — placeholder «Выберите топик».
  - Если топик выбран: только просмотр — фильтр сообщений, пагинация, таблица сообщений (MessagesTableSimple или аналог). Без кнопки «Добавить».

### Composables

- **useTopicsList(getApiKey)** (страница topics-messages):
  - Состояние: `topics`, `loadingTopics`, `topicsError`, пагинация (page, limit, total, pages), filter/sort (текущие значения инпутов).
  - Загрузка: `GET /api/topics?page=&limit=&filter=&sort=`, заголовки из credentialsStore.
  - Методы: loadTopics(page?, limit?), applyFilter(), setSort(), changePage(), showCurrentUrl().
  - При выборе строки топика вызывающий код берёт из элемента `topicId` и `dialogId` и передаёт в useMessagesByTopic.

- **useMessagesByTopic(getApiKey)**:
  - Вход: `dialogId` и `topicId` (ref или обычные значения). Если нет выбранного топика — не вызывать API.
  - Загрузка: `GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)&page=&limit=` (+ при необходимости sort).
  - Состояние: messages, loadingMessages, messagesError, пагинация, текущий фильтр сообщений (помимо topicId).
  - Методы: loadMessages(page?), applyMessageFilter(), changeMessagePage(), showCurrentMessageUrl().

### Переиспользование

- BasePanel, ExtendedPagination, BaseTable, стили таблиц — как на dialogs-messages.
- Таблица сообщений — тот же компонент MessagesTableSimple (или общий по смыслу), данные из useMessagesByTopic.
- Фильтр топиков — новый TopicFilterPanel с полями для topicId, dialogId, meta (и пресеты примеров), по аналогии с DialogFilterPanel.
- Модалка «Инфо» для топика — по аналогии с InfoModal на других страницах: показ полного JSON выбранного топика (topicId, dialogId, meta, createdAt и т.д.).

---

## 5. Порядок работ

1. **Backend:** добавить `GET /api/topics` в tenant-api (роут, topicController.getTenantTopics, фильтр по topicId, dialogId, meta; пагинация, обогащение meta).
2. **Роутер (controlo-ui):** маршрут `/topics-messages`, компонент страницы topics-messages.
3. **Sidebar:** пункт «Топики + Сообщения» с иконкой 📌 после «Диалоги + Сообщения», перед «Сообщения».
4. **Composables:** useTopicsList, useMessagesByTopic; подключить в странице.
5. **UI:** TopicsMessagesPage.vue — две колонки; левая — заголовок, TopicFilterPanel, пагинация, таблица топиков (topicId, dialogId, кнопка Инфо для JSON топика); правая — placeholder / фильтр сообщений + пагинация + MessagesTableSimple при выбранном топике.
6. **Проверка:** выбор топика → загрузка сообщений по topicId; пагинация и фильтр в обеих колонках; URL-кнопки.
