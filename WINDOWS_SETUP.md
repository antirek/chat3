# Инструкция по развертыванию Chat3 на Windows с Docker Desktop

## Предварительные требования

- Windows 10/11
- Docker Desktop установлен и запущен
- PowerShell или командная строка

## Пошаговая инструкция

### Шаг 1: Запустите Docker Desktop

1. Найдите "Docker Desktop" в меню Пуск
2. Запустите приложение
3. Дождитесь полного запуска (иконка в трее должна перестать мигать)
4. Проверьте, что Docker работает:
   ```powershell
   docker ps
   ```
   Должна отобразиться пустая таблица или список контейнеров (без ошибок)

### Шаг 2: Запустите MongoDB и RabbitMQ

Выполните в PowerShell:

```powershell
# Запустить MongoDB
docker run -d --name mongodb -p 27017:27017 -e MONGO_INITDB_DATABASE=chat3 mongo:latest

# Запустить RabbitMQ
docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 -e RABBITMQ_DEFAULT_USER=rmuser -e RABBITMQ_DEFAULT_PASS=rmpassword rabbitmq:3-management
```

Проверьте, что контейнеры запущены:
```powershell
docker ps
```

Вы должны увидеть два контейнера: `mongodb` и `rabbitmq`

### Шаг 3: Настройте docker-compose.yml

Поскольку MongoDB и RabbitMQ запущены в Docker контейнерах на той же машине, нужно заменить IP-адреса `192.168.0.42` на `host.docker.internal` (для доступа к хосту из контейнера).

Откройте файл `docker-compose.yml` и замените:

- Строка 17: `MONGODB_URI=mongodb://host.docker.internal:27017/chat3`
- Строка 18: `RABBITMQ_URL=amqp://rmuser:rmpassword@host.docker.internal:5672/`
- Строка 31: `MONGODB_URI=mongodb://host.docker.internal:27017/chat3`
- Строка 32: `RABBITMQ_URL=amqp://rmuser:rmpassword@host.docker.internal:5672/`

**Примечание:** `host.docker.internal` - это специальное имя хоста в Docker Desktop для Windows/Mac, которое позволяет контейнерам обращаться к сервисам на хосте.

### Шаг 4: Соберите Docker-образы

Перейдите в папку проекта и выполните:

```powershell
cd F:\Projects\chat3_admin
docker-compose build
```

Это может занять несколько минут при первом запуске.

### Шаг 5: Запустите контейнеры приложения

```powershell
docker-compose up -d
```

Проверьте статус контейнеров:
```powershell
docker-compose ps
```

Должны быть запущены:
- `chat3-api` (API сервер)
- `chat3-worker` (Worker для обработки событий)

### Шаг 6: Проверьте логи

```powershell
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f chat3-api
docker-compose logs -f chat3-worker
```

Нажмите `Ctrl+C` для выхода из просмотра логов.

### Шаг 7: Заполните БД тестовыми данными (опционально)

Если нужно заполнить базу данных тестовыми данными:

```powershell
docker exec -it chat3-api npm run seed
```

### Шаг 8: Сгенерируйте API ключ

**Важно:** API ключ необходим для работы с API и тестовыми интерфейсами.

Выполните одну из команд:

```powershell
# Генерация обычного API ключа (полные права)
docker exec -it chat3-api npm run generate-key

# Или генерация демо ключа (требует выполнения seed сначала)
docker exec -it chat3-api npm run generate-demo-key
```

Команда выведет сгенерированный API ключ (начинается с `chat3_`). **Скопируйте его** - он понадобится для работы с API.

**Пример вывода:**
```
✅ API Key generated successfully!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 Name:        Default API Key
📄 Description: (none)
🔑 API Key:     chat3_a5aab286afb089b16d185e425fc5bd9105b610b7e377b8e20f54d4e89bc99c76
🔐 Permissions: read, write, delete
✓  Active:      true
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Шаг 9: Проверьте работу приложения

Откройте в браузере:

- 🧪 **API Test Suite**: http://localhost:3002
- 📊 **Админ-панель**: http://localhost:3002/admin (не требует API ключа)
- 📚 **API Docs**: http://localhost:3002/api-docs
- 💚 **Health Check**: http://localhost:3002/health
- 🐰 **RabbitMQ Management**: http://localhost:15672
  - Логин: `rmuser`
  - Пароль: `rmpassword`

**Использование API ключа в тестовых интерфейсах:**

1. Откройте любой тестовый интерфейс (например, http://localhost:3002/api-test-users.html)
2. В поле "API Key" вставьте сгенерированный ключ (начинается с `chat3_`)
3. Нажмите "Установить" (если есть такая кнопка) или просто используйте интерфейс
4. Теперь вы можете выполнять API запросы через интерфейс

**Использование API ключа в Swagger UI:**

1. Откройте http://localhost:3002/api-docs
2. Нажмите кнопку "Authorize" (🔓) в правом верхнем углу
3. Вставьте ваш API ключ в поле `X-API-Key`
4. Нажмите "Authorize" и "Close"
5. Теперь вы можете тестировать API endpoints через Swagger UI

## Полезные команды

### Управление контейнерами

```powershell
# Остановить все контейнеры приложения
docker-compose down

# Остановить и удалить контейнеры
docker-compose down -v

# Перезапустить контейнеры
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart chat3-api

# Просмотр логов конкретного сервиса
docker-compose logs -f chat3-api
docker-compose logs -f chat3-worker
```

### Управление MongoDB и RabbitMQ

```powershell
# Остановить MongoDB и RabbitMQ
docker stop mongodb rabbitmq

# Запустить MongoDB и RabbitMQ
docker start mongodb rabbitmq

# Удалить контейнеры MongoDB и RabbitMQ
docker rm mongodb rabbitmq

# Просмотр логов MongoDB
docker logs mongodb

# Просмотр логов RabbitMQ
docker logs rabbitmq
```

### Hot Reload для разработки

**Для HTML файлов (src/public):** Hot reload уже настроен! 

Изменения в HTML файлах применяются автоматически без пересборки Docker образа:
1. Отредактируйте файл в `src/public/` (например, `api-test.html`)
2. Сохраните файл
3. Обновите страницу в браузере (F5 или Ctrl+R)
4. Изменения сразу видны!

**Для JavaScript кода (src/):** Hot reload также настроен, но для применения изменений в Node.js коде может потребоваться перезапуск контейнера:

```powershell
docker-compose restart chat3-api
```

### Пересборка после изменений в коде

Если вы изменили зависимости (`package.json`) или Dockerfile:

```powershell
# Пересобрать образы
docker-compose build

# Перезапустить с новыми образами
docker-compose up -d
```

## Важные замечания

1. **Порты:**
   - API сервер: `3002` (внешний) → `3000` (внутри контейнера)
   - MongoDB: `27017`
   - RabbitMQ AMQP: `5672`
   - RabbitMQ Management UI: `15672`

2. **Учетные данные RabbitMQ:**
   - Пользователь: `rmuser`
   - Пароль: `rmpassword`
   - Admin: `admin` / `admin` (для Management UI)

3. **Переменные окружения:**
   - Все настройки находятся в `docker-compose.yml`
   - MongoDB URI: `mongodb://host.docker.internal:27017/chat3`
   - RabbitMQ URL: `amqp://rmuser:rmpassword@host.docker.internal:5672/`

## Решение проблем

### Docker Desktop не запускается

1. Убедитесь, что Docker Desktop установлен
2. Перезагрузите компьютер
3. Проверьте, что виртуализация включена в BIOS

### Контейнеры не могут подключиться к MongoDB/RabbitMQ

1. Проверьте, что MongoDB и RabbitMQ запущены: `docker ps`
2. Убедитесь, что в `docker-compose.yml` используется `host.docker.internal` вместо IP адреса
3. Проверьте логи контейнеров: `docker-compose logs chat3-api` и `docker-compose logs chat3-worker`

### Ошибка "Invalid API key"

Если при работе с API или тестовыми интерфейсами появляется ошибка "Invalid API key":

1. **Убедитесь, что API ключ сгенерирован:**
   ```powershell
   docker exec -it chat3-api npm run generate-key
   ```

2. **Скопируйте сгенерированный ключ** (начинается с `chat3_`)

3. **Вставьте ключ в тестовый интерфейс:**
   - Откройте тестовый интерфейс (например, http://localhost:3002/api-test-users.html)
   - В поле "API Key" вставьте ваш ключ
   - Нажмите "Установить" (если есть такая кнопка)

4. **Для Swagger UI:**
   - Откройте http://localhost:3002/api-docs
   - Нажмите "Authorize" (🔓)
   - Вставьте ключ в поле `X-API-Key`
   - Нажмите "Authorize"

5. **Проверьте, что ключ активен:**
   ```powershell
   # Подключитесь к MongoDB и проверьте ключи
   docker exec -it mongodb mongosh chat3 --eval "db.apikeys.find().pretty()"
   ```

### Обновление дефолтного API ключа в тестовых интерфейсах

**Проблема:** Дефолтный ключ `chat3_a5aab286afb089b16d185e425fc5bd9105b610b7e377b8e20f54d4e89bc99c76` в HTML файлах может не существовать в вашей базе данных, поэтому приходится вводить ключ каждый раз.

**Решение:** Обновите дефолтный ключ в HTML файлах на ваш актуальный ключ:

1. **Сгенерируйте API ключ** (если еще не сделали):
   ```powershell
   docker exec -it chat3-api npm run generate-key
   ```

2. **Скопируйте сгенерированный ключ**

3. **Обновите дефолтный ключ в HTML файлах:**
   
   Откройте следующие файлы в редакторе и замените значение `value="chat3_a5aab286afb089b16d185e425fc5bd9105b610b7e377b8e20f54d4e89bc99c76"` на ваш новый ключ:
   
   - `src/public/api-test-users.html` (строка ~441)
   - `src/public/api-test-user-dialogs.html` (строка ~811)
   - `src/public/api-test-dialogs.html` (строка ~487)
   - `src/public/api-test-messages.html` (строка ~546)

   **Пример замены:**
   ```html
   <!-- Было: -->
   <input type="text" id="apiKey" ... value="chat3_a5aab286afb089b16d185e425fc5bd9105b610b7e377b8e20f54d4e89bc99c76">
   
   <!-- Стало (с вашим ключом): -->
   <input type="text" id="apiKey" ... value="chat3_ВАШ_НОВЫЙ_КЛЮЧ">
   ```

4. **Перезапустите контейнер** (чтобы применить изменения в docker-compose.yml):
   ```powershell
   docker-compose up -d
   ```

**Важно:** После первого раза вам больше **не нужно пересобирать Docker образ** для изменений в HTML файлах! 

В `docker-compose.yml` настроен hot reload для папки `src/public` - изменения в HTML файлах применяются автоматически. Просто сохраните файл и обновите страницу в браузере (F5 или Ctrl+R).

После этого ваш API ключ будет автоматически подставляться в тестовые интерфейсы, и вам не нужно будет вводить его каждый раз.

### Порт уже занят

Если порт 3002, 27017, 5672 или 15672 уже занят:

1. Найдите процесс, использующий порт:
   ```powershell
   netstat -ano | findstr :3002
   ```
2. Остановите процесс или измените порт в `docker-compose.yml`

## Остановка всех сервисов

Чтобы полностью остановить все сервисы:

```powershell
# Остановить контейнеры приложения
docker-compose down

# Остановить MongoDB и RabbitMQ
docker stop mongodb rabbitmq
```

## Запуск после остановки

Если вы остановили все сервисы и хотите запустить снова:

```powershell
# Запустить MongoDB и RabbitMQ
docker start mongodb rabbitmq

# Запустить контейнеры приложения
docker-compose up -d
```

