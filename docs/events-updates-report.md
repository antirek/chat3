# Анализ системы Events и Updates

## 📊 Общая статистика

| Показатель | Значение |
|-----------|----------|
| **Всего типов событий** | 11 |
| **Активно используется** | 11 (100%) |
| **Типов Updates** | 4 |
| **Событий на Update** | 1:1 (DialogUpdate), 1:N (MessageUpdate, DialogMemberUpdate) |

---

## 📋 Таблица типов Events

| Тип события | Описание | Где создается | Создает Update | Получатели |
|------------|----------|--------------|----------------|------------|
| `dialog.create` | Создан диалог | `dialogController.create` | `DialogUpdate` | Все участники |
| `dialog.update` | Обновлен диалог | `metaController.setMeta/deleteMeta` (для диалогов) | `DialogUpdate` | Все участники |
| `dialog.delete` | Удален диалог | `dialogController.delete` | `DialogUpdate` | Все участники |
| `message.create` | Создано сообщение | `messageController.create` | `MessageUpdate` | Все участники |
| `message.update` | Обновлено сообщение | `messageController.update`, `metaController.setMeta/deleteMeta` (для сообщений) | `MessageUpdate` | Все участники |
| `dialog.member.add` | Добавлен участник | `dialogMemberController.addMember` | `DialogUpdate` | Все участники |
| `dialog.member.remove` | Удален участник | `dialogMemberController.removeMember` | `DialogUpdate` | Все участники + удаляемый |
| `dialog.member.update` | Обновлен участник | `userDialogController.updateMessageStatus`, `dialogMemberController.updateMember`, `metaController.setMeta/deleteMeta` (для участников) | `DialogMemberUpdate` | Только этот участник | Изменение unreadCount, lastSeenAt или мета-тегов |
| `message.status.update` | Обновлен статус | `userDialogController.updateMessageStatus` | `MessageUpdate` | Все участники |
| `message.reaction.update` | Обновлена реакция | `messageReactionController.setOrUnsetReaction` | `MessageUpdate` | Все участники |
| `dialog.typing` | Пользователь печатает | `typingController.setTyping` | `TypingUpdate` | Все участники (кроме инициатора) |

---

## 📦 Структура секций в Events и Updates

### Секции Events (в поле `data`)

| Тип события | Секции в Event.data | Описание |
|------------|---------------------|----------|
| `dialog.create` | `context`, `dialog`, `actor` | Контекст события, данные диалога, информация об инициаторе |
| `dialog.update` | `context`, `dialog`, `actor` | Контекст события, обновленные данные диалога, информация об инициаторе |
| `dialog.delete` | `context`, `dialog`, `actor` | Контекст события, данные удаляемого диалога, информация об инициаторе |
| `message.create` | `context`, `dialog`, `message`, `actor` | Контекст события, данные диалога, полное сообщение, информация об отправителе |
| `message.update` | `context`, `dialog`, `message`, `actor` | Контекст события, данные диалога, обновленное сообщение, информация об инициаторе |
| `dialog.member.add` | `context`, `dialog`, `member`, `actor` | Контекст события, данные диалога, данные добавляемого участника, информация об инициаторе |
| `dialog.member.remove` | `context`, `dialog`, `member`, `actor` | Контекст события, данные диалога, данные удаляемого участника, информация об инициаторе |
| `dialog.member.update` | `context`, `dialog`, `member`, `actor` | Контекст события, данные диалога, обновленные данные участника, информация об инициаторе |
| `message.status.update` | `context`, `dialog`, `message`, `member`, `actor` | Контекст события, данные диалога, данные сообщения со статусом, данные участника, информация об инициаторе |
| `message.reaction.update` | `context`, `dialog`, `message`, `member`, `actor` | Контекст события, данные диалога, данные сообщения с реакцией, данные участника, информация об инициаторе |
| `dialog.typing` | `context`, `dialog`, `typing`, `member`, `actor` | Контекст события, данные диалога, данные о печатании, данные участника, информация об инициаторе |

**Примечания:**
- `context` - всегда присутствует, содержит метаданные события (eventType, dialogId, entityId, includedSections, updatedFields)
- `actor` - информация об инициаторе события (actorId, actorType, info)
- `dialog` - данные диалога (dialogId, tenantId, name, meta и т.д.)
- `member` - данные участника диалога (userId, meta, state: {unreadCount, lastSeenAt, ...})
- `message` - данные сообщения (messageId, content, type, meta, statuses, reactionCounts и т.д.)
- `typing` - данные о печатании (userId, expiresInMs, timestamp, userInfo)

### Секции Updates (в поле `data`)

| Тип Update | Секции в Update.data | Описание |
|-----------|---------------------|----------|
| **DialogUpdate** | `context`, `dialog`, `member` | Контекст события, данные диалога, данные текущего участника (для которого создан update) |
| **DialogMemberUpdate** | `context`, `dialog`, `member` | Контекст события, данные диалога, обновленные данные конкретного участника |
| **MessageUpdate** | `context`, `dialog`, `member`, `message` | Контекст события, данные диалога, данные текущего участника, данные сообщения (полное или частичное) |
| **TypingUpdate** | `context`, `dialog`, `member`, `typing` | Контекст события, данные диалога, данные текущего участника, данные о печатании |

**Примечания:**
- `context` - всегда присутствует, содержит метаданные события
- `dialog` - данные диалога (для контекста)
- `member` - данные участника, для которого создан update (его персональные данные)
- `message` - данные сообщения (может быть полным или содержать только изменения: statusUpdate, reactionUpdate)
- `typing` - данные о печатании (временное уведомление)

### Детальное описание секций

#### Секция `context`
```javascript
{
  version: 2,
  eventType: 'message.create',
  dialogId: 'dlg_...',
  entityId: 'msg_...',
  messageId: 'msg_...', // опционально
  includedSections: ['dialog', 'message', 'actor'],
  updatedFields: ['message']
}
```

#### Секция `dialog`
```javascript
{
  dialogId: 'dlg_...',
  tenantId: 'tnt_...',
  name: 'Dialog Name', // опционально
  createdBy: 'user_id',
  createdAt: 1234567890.123456,
  updatedAt: 1234567890.123456,
  meta: { /* мета-теги диалога */ }
}
```

#### Секция `member`
```javascript
{
  userId: 'user_id',
  meta: { /* мета-теги участника */ },
  state: {
    unreadCount: 5,
    lastSeenAt: 1234567890.123456,
    lastMessageAt: 1234567890.123456,
    isActive: true
  }
}
```

#### Секция `message`
```javascript
{
  messageId: 'msg_...',
  dialogId: 'dlg_...',
  senderId: 'user_id',
  type: 'internal.text',
  content: 'Message text',
  meta: { /* мета-теги сообщения */ },
  statuses: [ /* массив статусов */ ],
  reactionCounts: { '👍': 3 },
  // Опционально для частичных updates:
  statusUpdate: { userId, status, oldStatus },
  reactionUpdate: { userId, reaction, oldReaction, counts }
}
```

#### Секция `typing`
```javascript
{
  userId: 'user_id',
  expiresInMs: 5000,
  timestamp: 1234567890,
  userInfo: { /* информация о пользователе */ }
}
```

#### Секция `actor`
```javascript
{
  actorId: 'user_id',
  actorType: 'user', // 'user' | 'bot' | 'api' | 'system'
  info: { /* дополнительная информация об инициаторе */ }
}
```

---

## 🔄 Типы Updates и их источники

| Тип Update | События-источники | Получатели | Особенности |
|-----------|-------------------|------------|-------------|
| **DialogUpdate** | `dialog.create`, `dialog.update`, `dialog.delete`, `dialog.member.add`, `dialog.member.remove` | Все активные участники диалога | При `dialog.member.remove` также создается для удаляемого пользователя |
| **DialogMemberUpdate** | `dialog.member.update` | Только конкретный участник | Персонализированный update для одного пользователя |
| **MessageUpdate** | `message.create`, `message.update`, `message.status.update`, `message.reaction.update` | Все активные участники диалога | Может содержать полное сообщение или только изменения (status/reaction) |
| **TypingUpdate** | `dialog.typing` | Все участники кроме инициатора | Временное событие с expiresInMs |

---

## 🔗 Маппинг событий на Updates

### DialogUpdate Events
```javascript
DIALOG_UPDATE_EVENTS = [
  'dialog.create',
  'dialog.update',
  'dialog.delete',
  'dialog.member.add',
  'dialog.member.remove'
]
```

### DialogMemberUpdate Events
```javascript
DIALOG_MEMBER_UPDATE_EVENTS = [
  'dialog.member.update'
]
```

### MessageUpdate Events
```javascript
MESSAGE_UPDATE_EVENTS = [
  'message.create',
  'message.update',
  'message.reaction.update',
  'message.status.update'
]
```

### TypingUpdate Events
```javascript
TYPING_EVENTS = [
  'dialog.typing'
]
```

---

## ⚡ Поток обработки

```
1. Контроллер создает Event → MongoDB
2. Event публикуется в RabbitMQ (асинхронно)
3. Update Worker подписывается на события
4. Update Worker вызывает соответствующую функцию create*Update()
5. Update создается для каждого получателя → MongoDB
6. Update публикуется в RabbitMQ с routing key: user.{type}.{userId}.{updateType}
```

---

## 🎯 Новые возможности

### ✅ Реализовано
1. **Генерация событий при обновлении мета-тегов**
   - `message.update` при изменении мета-тегов сообщений
   - `dialog.member.update` при изменении мета-тегов участников
   - `dialog.update` при изменении мета-тегов диалогов

2. **История статусов сообщений**
   - `MessageStatus` теперь хранит историю изменений
   - Каждое изменение создает новую запись
   - `message.status.update` генерируется для всех изменений

3. **Унифицированные события реакций**
   - `message.reaction.update` заменяет `add`/`remove`
   - Позволяет множественные реакции от одного пользователя

### 💡 Потенциальные улучшения
1. **Батчинг Updates**
   - Группировка нескольких Updates для одного пользователя
   - Снижение нагрузки на RabbitMQ

2. **Условная генерация Updates**
   - Пропуск Updates для неактивных пользователей
   - Фильтрация по настройкам уведомлений

3. **Дельта-обновления**
   - Отправка только измененных полей вместо полных объектов
   - Снижение размера payload

---

## ⚠️ Проблемы и ограничения

### 1. Производительность
| Проблема | Описание | Влияние |
|----------|----------|---------|
| **N+1 запросы мета-тегов** | При создании Update для каждого участника запрашиваются мета-теги отдельно | Высокая нагрузка на БД при большом количестве участников |
| **Дублирование данных** | Каждый Update содержит полную копию dialog/message/member | Увеличение размера БД и RabbitMQ |
| **Синхронная публикация** | Создание Updates блокирует основной поток | Задержки в ответах API |

### 2. Консистентность
| Проблема | Описание | Риск |
|----------|----------|------|
| **Race conditions** | Event может быть обработан до сохранения в БД | Потеря Updates |
| **Частичная публикация** | Если RabbitMQ недоступен, Updates создаются, но не публикуются | Накопление неопубликованных Updates |

### 3. Масштабируемость
| Проблема | Описание | Решение |
|----------|----------|---------|
| **Линейный рост Updates** | Для каждого события создается N Updates (N = количество участников) | Батчинг, условная генерация |
| **Индексы БД** | Множественные индексы на Update могут замедлять запись | Оптимизация индексов, партиционирование |

---

## 🔧 Рекомендации по оптимизации

### Краткосрочные (1-2 недели)
1. ✅ **Кэширование мета-тегов** при создании Updates
2. ✅ **Асинхронная публикация Updates** в RabbitMQ (уже реализовано)
3. ✅ **Оптимизация индексов** на Update (проверить использование)

### Среднесрочные (1-2 месяца)
1. **Батчинг Updates**
   - Группировка Updates по userId и dialogId
   - Отправка одного сообщения с массивом Updates

2. **Условная генерация**
   - Проверка активности пользователя перед созданием Update
   - Учет настроек уведомлений пользователя

3. **Дельта-обновления**
   - Отправка только измененных полей в `updatedFields`
   - Клиент применяет изменения к локальному состоянию

### Долгосрочные (3+ месяца)
1. **Event Sourcing**
   - Хранение только событий, Updates генерируются по запросу
   - Снижение размера БД

2. **Партиционирование Updates**
   - Разделение по tenantId или userId
   - Улучшение производительности запросов

3. **Сжатие данных**
   - Gzip для больших Updates
   - Снижение нагрузки на сеть и RabbitMQ

---

## 📈 Метрики для мониторинга

| Метрика | Целевое значение | Текущее состояние |
|---------|------------------|-------------------|
| **Время создания Event** | < 50ms | ? |
| **Время создания Update** | < 100ms | ? |
| **Время публикации в RabbitMQ** | < 200ms | ? |
| **Количество неопубликованных Updates** | < 1000 | ? |
| **Размер Update в БД** | < 10KB | ? |
| **Количество Updates на событие** | = количество участников | ✅ |

---

## 🎓 Выводы

### Сильные стороны
- ✅ Полное покрытие всех операций событиями
- ✅ Четкое разделение типов Updates
- ✅ Гибкая система мета-тегов с генерацией событий
- ✅ История статусов сообщений

### Области для улучшения
- ⚠️ Производительность при большом количестве участников
- ⚠️ Размер данных в Updates
- ⚠️ Обработка ошибок публикации

### Приоритеты
1. **Высокий**: Оптимизация запросов мета-тегов (кэширование)
2. **Средний**: Батчинг Updates
3. **Низкий**: Дельта-обновления

---

## 🤔 Анализ необходимости отдельного TypingUpdate

### Текущая реализация

**TypingUpdate** - отдельный тип Update для события `dialog.typing`:
- Имеет собственную структуру с секцией `typing`
- Публикуется в RabbitMQ с routing key `user.{type}.{userId}.typing`
- Исключает инициатора из получателей
- Содержит временные данные (`expiresInMs`, `timestamp`)

### Особенности TypingUpdate

| Характеристика | TypingUpdate | DialogUpdate |
|----------------|--------------|--------------|
| **Семантика** | Временное уведомление о действии | Изменение состояния диалога |
| **Длительность** | Эфемерное (expiresInMs ~5 сек) | Постоянное (сохраняется в истории) |
| **Получатели** | Все кроме инициатора | Все участники |
| **Структура данных** | `{dialog, member, typing}` | `{dialog, member}` |
| **Routing key** | `user.{type}.{userId}.typing` | `user.{type}.{userId}.dialogupdate` |

### Варианты решения

#### Вариант 1: Оставить отдельным (текущий) ✅

**Плюсы:**
- ✅ Четкое разделение семантики (действие vs состояние)
- ✅ Разные routing keys позволяют клиентам подписываться отдельно
- ✅ Клиент может игнорировать typing updates, если не нужны
- ✅ Проще фильтровать и обрабатывать временные события
- ✅ Не засоряет историю Updates постоянными записями

**Минусы:**
- ❌ Дополнительный код для обработки отдельного типа
- ❌ Дублирование логики (получение участников, мета-тегов)
- ❌ Больше типов Updates для поддержки

#### Вариант 2: Объединить с DialogUpdate

**Плюсы:**
- ✅ Меньше типов Updates (упрощение кода)
- ✅ Единая логика обработки
- ✅ Меньше дублирования кода

**Минусы:**
- ❌ Смешивание семантики (действие + состояние)
- ❌ Клиенты получают typing updates даже если не нужны
- ❌ Сложнее фильтровать временные события
- ❌ Typing сохраняется в истории Updates (не нужно)
- ❌ Нужно менять routing key или добавлять фильтрацию

#### Вариант 3: Опциональная секция в DialogUpdate

**Плюсы:**
- ✅ Единый тип Update
- ✅ Можно включать/исключать typing секцию
- ✅ Гибкость в обработке

**Минусы:**
- ❌ Усложнение структуры DialogUpdate
- ❌ Нужна логика условного включения секции
- ❌ Все равно сохраняется в истории

### Рекомендация

**Оставить TypingUpdate отдельным типом** по следующим причинам:

1. **Семантическая чистота**
   - Typing - это уведомление о действии, а не изменение состояния
   - DialogUpdate - это изменения состояния диалога
   - Разделение улучшает понимание системы

2. **Производительность**
   - Клиенты могут подписаться только на нужные типы updates
   - Не нужно обрабатывать typing, если не требуется
   - Разные routing keys позволяют оптимизировать подписки

3. **История и хранение**
   - Typing updates не должны сохраняться в истории (эфемерные)
   - DialogUpdate сохраняется для аудита и восстановления состояния
   - Разделение позволяет разную политику хранения

4. **Обработка на клиенте**
   - Клиент может обрабатывать typing отдельно (показ индикатора)
   - Не нужно парсить DialogUpdate для проверки наличия typing
   - Проще реализовать автоскрытие индикатора через expiresInMs

### Альтернативное решение (если нужна оптимизация)

Если количество типов Updates критично, можно:
1. **Создать общий тип `NotificationUpdate`** для временных уведомлений:
   - `dialog.typing` → `NotificationUpdate` (тип: 'typing')
   - В будущем: `user.online`, `user.away` и т.д.
2. **Использовать флаг `ephemeral: true`** в Update модели
3. **Автоматически удалять ephemeral updates** после истечения

Это сохранит семантическое разделение, но уменьшит количество типов.

---

## 🔍 Сравнение DialogUpdate и DialogMemberUpdate

### Ключевые различия

| Характеристика | DialogUpdate | DialogMemberUpdate |
|----------------|-------------|-------------------|
| **Получатели** | Все активные участники диалога | Только один конкретный участник |
| **События-источники** | `dialog.create`, `dialog.update`, `dialog.delete`, `dialog.member.add`, `dialog.member.remove` | `dialog.member.update` |
| **Семантика** | Изменение состава/состояния диалога | Изменение состояния конкретного участника |
| **entityId** | `dialogId` (диалог) | `dialogId` (диалог) |
| **Количество Updates** | N (по количеству участников) | 1 (только для изменяемого участника) |
| **Секции в data** | `{dialog, member}` | `{dialog, member}` |
| **Routing key** | `user.{type}.{userId}.dialogupdate` | `user.{type}.{userId}.dialogmemberupdate` |

### Почему `dialog.member.add` создает DialogUpdate?

#### Логика разделения:

**DialogUpdate** используется для событий, которые **затрагивают всех участников диалога**:
- ✅ `dialog.create` - все должны знать о новом диалоге
- ✅ `dialog.update` - все должны получить обновленную информацию о диалоге
- ✅ `dialog.delete` - все должны знать об удалении диалога
- ✅ `dialog.member.add` - **все участники должны обновить список участников**
- ✅ `dialog.member.remove` - **все участники должны обновить список участников**

**DialogMemberUpdate** используется для событий, которые **касаются только одного участника**:
- ✅ `dialog.member.update` - изменение `unreadCount`, `lastSeenAt`, **мета-тегов** конкретного участника

#### Пример: `dialog.member.add`

Когда добавляется новый участник (`dialog.member.add`):

1. **Все участники должны знать:**
   - Кто добавился в диалог
   - Обновленный список участников
   - Новый состав диалога

2. **Создается DialogUpdate для каждого участника:**
   ```javascript
   // Для каждого участника (включая нового)
   {
     dialog: { dialogId, ... },
     member: { userId: 'current_user', ... }, // данные текущего участника
     context: {
       eventType: 'dialog.member.add',
       // ...
     }
   }
   ```

3. **Каждый участник получает:**
   - Актуальную информацию о диалоге
   - Свои данные как участника
   - Контекст события (что произошло)

#### Пример: `dialog.member.update`

Когда изменяется состояние участника (`dialog.member.update`):

1. **Только этот участник должен знать:**
   - Свой обновленный `unreadCount`
   - Свой обновленный `lastSeenAt`
   - Свои обновленные мета-теги

2. **Создается DialogMemberUpdate только для этого участника:**
   ```javascript
   {
     dialog: { dialogId, ... },
     member: { 
       userId: 'updated_user',
       state: { unreadCount: 5, lastSeenAt: ... }
     },
     context: {
       eventType: 'dialog.member.update',
       updatedFields: ['member.state.unreadCount']
     }
   }
   ```

3. **Другие участники не получают update:**
   - Изменение `unreadCount` другого участника не касается остальных
   - Это персональная информация каждого участника

### Альтернативный подход (не используется)

Можно было бы создать DialogMemberUpdate для `dialog.member.add`:
- ❌ Но тогда нужно создавать Update для **всех** участников отдельно
- ❌ Это дублирует логику DialogUpdate
- ❌ Семантически неправильно: `dialog.member.add` - это изменение диалога, а не участника

### Вывод

**Разделение логично и правильно:**

1. **DialogUpdate** = изменения, которые **все должны знать**
   - Состав диалога (add/remove)
   - Состояние диалога (create/update/delete)

2. **DialogMemberUpdate** = изменения, которые **касаются только одного**
   - Персональное состояние участника (unreadCount, lastSeenAt)
   - **Персональные мета-теги участника** (через `metaController.setMeta/deleteMeta` с `entityType='dialogMember'`)

### Когда генерируется `dialog.member.update`?

Событие `dialog.member.update` создается в трех случаях:

1. **Изменение unreadCount/lastSeenAt** (`userDialogController.updateMessageStatus`)
   - При обновлении статуса сообщения
   - Обновляется счетчик непрочитанных и время последнего просмотра

2. **Изменение состояния участника** (`dialogMemberController.updateMember`)
   - Прямое обновление через API
   - Изменение роли, статуса активности и т.д.

3. **Изменение мета-тегов участника** (`metaController.setMeta/deleteMeta` с `entityType='dialogMember'`)
   - При установке/удалении мета-тегов для DialogMember
   - `entityId` имеет формат: `dialogId:userId`
   - Создается событие с `updatedFields: ['member.meta']`

3. **`dialog.member.add` в DialogUpdate** потому что:
   - Это изменение **состава диалога**
   - Все участники должны обновить свой список участников
   - Это не персональное изменение, а изменение диалога

---

*Отчет создан: 2025-01-27*
*Версия системы: 0.0.12*

