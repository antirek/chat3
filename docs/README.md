# Документация Chat3

Добро пожаловать в документацию Chat3 - системы управления чатами с поддержкой мультитенантности и событийно-ориентированной архитектуры.

## 📚 Содержание документации

### [ARCHITECTURE.md](ARCHITECTURE.md)
Архитектура системы с диаграммами Mermaid:
- Компоненты системы
- Поток данных
- RabbitMQ архитектура
- Мультитенантность
- Безопасность
- Мета-теги
- Типы пользователей

### [API.md](API.md)
Полное описание REST API:
- Аутентификация
- Формат ответов
- Пагинация и фильтрация
- Все endpoints с примерами
- Коды ответов

### [EVENTS.md](EVENTS.md)
Система событий:
- Модель Event
- Все типы событий
- Структура данных событий
- RabbitMQ routing keys
- Обработка событий

### [UPDATES.md](UPDATES.md)
Система обновлений:
- Модель Update
- Типы обновлений
- RabbitMQ routing keys для updates
- Создание и получение updates
- Примеры обработки

### [INTEGRATION.md](INTEGRATION.md)
Интеграция для внешних систем:
- Подключение к RabbitMQ
- Подписка на обновления
- Обработка различных типов обновлений
- Полные примеры кода
- Best practices
- Тестирование интеграции

## 🚀 Быстрый старт

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Запуск сервисов:**
   ```bash
   docker-compose up -d  # MongoDB и RabbitMQ
   npm run start:tenant-api  # Tenant API (основной REST API)
   npm run start:admin-web  # AdminJS интерфейс
   npm run start:control-api  # Control API (служебный API)
   npm run start:api-test  # API Test интерфейс
   npm run start:updateWorker  # Update Worker
   # Или запустить все сразу:
   npm run start:all
   ```

3. **Генерация API ключа:**
   ```bash
   npm run generate-key
   ```

4. **Доступ к сервисам:**
   - **Tenant API** (порт 3000): 
     - Swagger UI: http://localhost:3000/api-docs
     - Health Check: http://localhost:3000/health
   - **Admin Web** (порт 3001):
     - AdminJS: http://localhost:3001/admin
     - Quick Links: http://localhost:3001/admin-links
   - **Control API** (порт 3002):
     - Health Check: http://localhost:3002/health
   - **API Test** (порт 3003):
     - Главная страница: http://localhost:3003

## 🔑 Ключевые концепции

### Мультитенантность
Все данные изолированы по `tenantId`. По умолчанию используется `tnt_default`.

### События
Все изменения в системе генерируют события, которые:
- Сохраняются в MongoDB
- Публикуются в RabbitMQ exchange `chat3_events`

### Обновления
На основе событий создаются персонализированные обновления для пользователей:
- Сохраняются в MongoDB
- Публикуются в RabbitMQ exchange `chat3_updates`
- Routing key: `user.{type}.{userId}.{updateType}`

### Мета-теги
Любая сущность может иметь мета-теги:
- Поддержка scope для персональных значений
- Приоритет scoped значений при получении

### Типы пользователей
Пользователи имеют поле `type`:
- `user` (по умолчанию)
- `bot`
- `contact`
- `agent`
- и другие

Тип используется в RabbitMQ routing keys.

## 📖 Примеры использования

### Создание диалога с участниками

```bash
curl -X POST http://localhost:3000/api/dialogs \
  -H "X-API-Key: your-key" \
  -H "X-Tenant-ID: tnt_default" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "VIP чат",
    "createdBy": "carl",
    "members": [
      {"userId": "carl", "type": "user", "name": "Carl Johnson"},
      {"userId": "bot_123", "type": "bot", "name": "Support Bot"}
    ]
  }'
```

### Подписка на обновления пользователя

```javascript
import amqp from 'amqplib';

const connection = await amqp.connect('amqp://rmuser:rmpassword@localhost:5672/');
const channel = await connection.createChannel();

const queueName = 'user.user.carl';
await channel.assertQueue(queueName, {
  durable: true,
  arguments: { 'x-message-ttl': 3600000 }
});

await channel.bindQueue(queueName, 'chat3_updates', 'user.user.carl.*');

channel.consume(queueName, (msg) => {
  const update = JSON.parse(msg.content.toString());
  console.log('Update:', update.eventType);
  channel.ack(msg);
});
```

## 🔗 Полезные ссылки

- [Swagger UI](http://localhost:3000/api-docs) - Интерактивная API документация (Tenant API)
- [AdminJS](http://localhost:3001/admin) - Административная панель (Admin Web)
- [API Test Suite](http://localhost:3003) - Интерфейс для тестирования API
- [Health Check](http://localhost:3000/health) - Статус Tenant API

## 📝 Примечания

- Все timestamps используют микросекундную точность
- RabbitMQ является обязательным компонентом
- API ключи требуются для всех запросов
- Updates имеют TTL 1 час в RabbitMQ очередях

## 🤝 Поддержка

Для вопросов и проблем обращайтесь к соответствующей документации:
- Архитектура → [ARCHITECTURE.md](ARCHITECTURE.md)
- API → [API.md](API.md)
- События → [EVENTS.md](EVENTS.md)
- Обновления → [UPDATES.md](UPDATES.md)
- Интеграция → [INTEGRATION.md](INTEGRATION.md)

