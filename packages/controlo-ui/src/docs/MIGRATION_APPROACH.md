# Подход к миграции: HTML → Vue → Рефакторинг

## Стратегия

Вместо построения всего с нуля по FSD, используем итеративный подход:

1. **Этап 1**: Перенести HTML в Vue компоненты (сохранить функциональность)
2. **Этап 2**: Рефакторинг - вынести переиспользуемые части в FSD структуру

## Преимущества подхода

✅ **Быстрее получить рабочий результат** - страницы работают сразу
✅ **Меньше риск потерять функциональность** - переносим существующий код
✅ **Итеративная оптимизация** - улучшаем постепенно
✅ **Легче тестировать** - каждая страница работает независимо

## План действий

### Фаза 1: Перенос HTML → Vue (1-2 дня на страницу)

Для каждой страницы:

1. **Создать Vue компонент** в `src/pages/`
2. **Перенести HTML структуру** в `<template>`
3. **Перенести стили** в `<style scoped>`
4. **Перенести JavaScript логику** в `<script setup>`
5. **Адаптировать под Vue**:
   - Заменить `document.getElementById` на refs
   - Заменить `innerHTML` на реактивные данные
   - Заменить события на `@click`, `@input` и т.д.
   - Использовать `v-if`, `v-for`, `v-model`

### Фаза 2: Рефакторинг (по мере необходимости)

После переноса всех страниц:

1. **Вынести общие стили** → `shared/ui/`
2. **Вынести переиспользуемые функции** → `shared/lib/utils/`
3. **Создать базовые компоненты** → `shared/ui/` (Button, Modal, Table)
4. **Создать Column Feature** → `features/column/`
5. **Создать Entities** → `entities/tenant/`, `entities/user/` и т.д.
6. **Перенести страницы в Widgets** → `widgets/tenants-page/` и т.д.

## Порядок переноса страниц

### Приоритет 1: Простые страницы (для понимания паттернов)
1. ✅ `api-test.html` → Главная страница (навигация) → `AppLayout.vue`
2. ✅ `api-test-tenants.html` → `TenantsPage.vue` (полностью реализовано)
3. ✅ `api-test-users.html` → `UsersPage.vue` (полностью реализовано)
4. ✅ `api-test-messages.html` → `MessagesPage.vue` (полностью реализовано)

### Приоритет 2: Средние страницы
5. ✅ `api-test-dialogs.html` → `DialogsMessagesPage.vue` (полностью реализовано)
6. ⏳ `api-test-init.html` → `InitPage.vue` (заглушка)
7. ⏳ `api-test-events-updates.html` → `EventsUpdatesPage.vue` (заглушка)

### Приоритет 3: Сложные страницы
8. ⏳ `api-test-user-dialogs.html` → `UserDialogsPage.vue` (заглушка, 3 колонки)
9. ⏳ `api-test-db-explorer.html` → `DbExplorerPage.vue` (заглушка, специфичная логика)

## Шаблон переноса

### Структура Vue компонента

```vue
<template>
  <!-- HTML структура из оригинального файла -->
  <div class="page-container">
    <!-- ... -->
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted, computed } from 'vue';

// Переносим переменные из let/const
const apiKey = ref('');
const tenantId = ref('tnt_default');
const currentPage = ref(1);
const currentLimit = ref(20);
const data = ref([]);
const loading = ref(false);

// Переносим функции
async function loadData() {
  loading.value = true;
  try {
    // Логика из оригинальной функции
  } finally {
    loading.value = false;
  }
}

onMounted(() => {
  // Инициализация
  loadData();
});
</script>

<style scoped>
/* Переносим стили из <style> блока */
</style>
```

## Что переносить

### ✅ Переносим:
- HTML структуру
- CSS стили
- JavaScript логику (функции, обработчики)
- API вызовы
- Состояние (переменные)

### 🔄 Адаптируем:
- DOM манипуляции → реактивные данные
- `getElementById` → refs
- `addEventListener` → `@click`, `@input`
- `innerHTML` → `v-html` или шаблоны
- События → Vue events

### ❌ Не переносим сразу:
- Дублирующийся код (вынесем позже)
- Глобальные функции (сделаем composables)
- Инлайн стили (вынесем в компоненты)

## Пример: api-test-tenants.html → TenantsPage.vue

### Шаги:

1. **Скопировать HTML** в `<template>`
2. **Заменить статические данные** на `v-for`, `v-if`
3. **Заменить обработчики** на `@click`, `@input`
4. **Перенести функции** в `<script setup>`
5. **Адаптировать под Vue реактивность**

### Ключевые изменения:

```javascript
// Было (vanilla JS):
let tenants = [];
function loadTenants() {
  fetch('/api/tenants').then(r => r.json()).then(data => {
    tenants = data.items;
    renderTable();
  });
}

// Стало (Vue):
const tenants = ref([]);
async function loadTenants() {
  const response = await fetch('/api/tenants');
  const data = await response.json();
  tenants.value = data.items;
}
```

## После переноса всех страниц

Когда все страницы работают в Vue:

1. **Анализ дублирования** - найти общие паттерны
2. **Создание базовых компонентов** - Button, Modal, Table
3. **Создание Column Feature** - переиспользуемая колонка
4. **Создание Entities** - бизнес-логика сущностей
5. **Рефакторинг страниц** - использовать новые компоненты

## Преимущества этого подхода

1. **Быстрый результат** - страницы работают сразу
2. **Меньше риск** - не теряем функциональность
3. **Итеративность** - улучшаем постепенно
4. **Гибкость** - можем остановиться на любом этапе
5. **Обратная совместимость** - старые HTML файлы остаются как reference

## Недостатки

1. **Временный технический долг** - дублирование кода
2. **Не сразу FSD** - структура появится после рефакторинга
3. **Больше работы** - нужно делать рефакторинг позже

## Рекомендация

✅ **Использовать этот подход**, потому что:
- Быстрее получить рабочий результат
- Меньше риск потерять функциональность
- Можно итеративно улучшать
- Легче тестировать каждую страницу
