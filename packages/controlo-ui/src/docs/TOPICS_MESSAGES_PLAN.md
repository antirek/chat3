# План: страница «Топики + Сообщения»

## Цель

Добавить в sidebar новый пункт **«Топики + Сообщения»** после «Диалоги + Сообщения» и перед «Сообщения».

- **Колонка 1:** заголовок, фильтр, пагинация, таблица по топикам.
- **Колонка 2:** при клике по топику — сообщения этого топика (фильтр, пагинация, таблица).

---

## Анализ API (tenant-api)

### Топики

| Эндпоинт | Назначение |
|----------|------------|
| `GET /api/dialogs/:dialogId/topics` | Список топиков **одного диалога** (page, limit, filter, sort). Ответ: `{ data: [], pagination }`. |
| `GET /api/dialogs/:dialogId/topics/:topicId` | Один топик по ID. |
| `POST /api/dialogs/:dialogId/topics` | Создать топик. |
| `PATCH /api/dialogs/:dialogId/topics/:topicId` | Обновить топик (meta). |

**Важно:** эндпоинта уровня тенанта вида `GET /api/topics` (все топики по всем диалогам) **нет**.

### Сообщения по топику

| Эндпоинт | Назначение |
|----------|------------|
| `GET /api/dialogs/:dialogId/messages` | Список сообщений диалога. Поддерживает фильтр по `topicId`. |

Формат фильтра (queryParser): `(topicId,eq,topic_xxxxxxxxxxxxxxxxxxxx)` или `(topicId,eq,null)` для сообщений без топика. В контроллере обрабатывается `regularFilters.topicId`.

Итого: чтобы показать сообщения выбранного топика, нужны **dialogId** и **topicId**. Запрос:  
`GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)&page=1&limit=10`.

---

## Проблема: откуда брать список топиков для первой колонки

Сейчас топики в API привязаны к диалогу. Для страницы «Топики + Сообщения» возможны два подхода.

### Вариант A: новый API — список топиков по тенанту

- Добавить в tenant-api: **`GET /api/topics`** (tenant-level).
- Параметры: `page`, `limit`, `filter`, `sort` (аналогично диалогам/пользователям).
- В каждом элементе обязательно: `topicId`, `dialogId`, опционально `meta`, `createdAt`.
- Тогда сценарий: колонка 1 — таблица из `GET /api/topics`; при клике по строке есть `topicId` и `dialogId` → колонка 2 — `GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)`.

Плюсы: одна страница с двумя колонками, без предвыбора диалога. Минусы: нужна доработка бэкенда.

### Вариант B: без нового API — сначала выбор диалога

- На странице «Топики + Сообщения» сначала выбирается диалог (например, выпадающий список или компактная верхняя панель с выбором диалога).
- Колонка 1: топики выбранного диалога — `GET /api/dialogs/:dialogId/topics`.
- Колонка 2: при выборе топика — `GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)`.

Плюсы: реализуется только во frontend, текущего API достаточно. Минусы: лишний шаг (сначала выбрать диалог).

---

## Вопросы к тебе

1. **Вариант A или B?**  
   - A — делаем `GET /api/topics` и страницу «все топики → сообщения топика».  
   - B — без нового API: выбор диалога → топики диалога → сообщения топика.

2. **Таблица топиков (колонка 1):**  
   - При варианте A нужно показывать `dialogId` (и/или мету/название диалога)?  
   - Нужны ли кнопки/действия: «URL», «Мета», «Сообщения» (последнее по смыслу уже «выбор строки»)?

3. **Правая колонка (сообщения):**  
   - Поведение как на «Диалоги + Сообщения»: только просмотр + фильтр/пагинация/таблица, без кнопки «Добавить» или с ней?

4. **Фильтр топиков:**  
   - При варианте A — какие поля планируем в фильтре (topicId, dialogId, meta.*)?  
   - При варианте B фильтр уже есть в `GET /api/dialogs/:dialogId/topics` (query: filter, sort) — используем как в dialogs-messages?

5. **Иконка и маршрут:**  
   - Маршрут: например `/topics-messages`.  
   - Иконка в sidebar: например 📌 (как топики) или другая?

---

## Предлагаемая структура (после выбора варианта)

### Роутинг и sidebar

- **Маршрут:** `/topics-messages`, name: `topics-messages`.
- **Sidebar (AppLayout.vue):** после «Диалоги + Сообщения», перед «Сообщения» — пункт с иконкой и title «Топики + Сообщения», `to="/topics-messages"`.

### Страница (2 колонки)

- **Левая панель (BasePanel ~50%):**
  - Заголовок: «Топики» (и при варианте B — выбор диалога).
  - Фильтр (TopicFilterPanel / выбор диалога).
  - Пагинация (ExtendedPagination или аналог).
  - Таблица топиков (TopicTable с колонками topicId, при A — dialogId, meta; клик по строке = выбор топика).

- **Правая панель (BasePanel ~50%):**
  - Заголовок: «Сообщения» (при выбранном топике).
  - Placeholder, если топик не выбран: «Выберите топик».
  - При выбранном топике: фильтр сообщений, пагинация, таблица сообщений (MessagesTableSimple или аналог), запрос `GET /api/dialogs/:dialogId/messages?filter=(topicId,eq,TOPIC_ID)`.

### Модель (composables)

- **Вариант A:**
  - `useTopicsList(getApiKey)` — загрузка `GET /api/topics`, состояние: список, пагинация, фильтр, сортировка.
  - `useMessagesByTopic(getApiKey)` — при `dialogId` + `topicId`: загрузка сообщений с фильтром по topicId, пагинация.
- **Вариант B:**
  - Выбор диалога: либо существующий `useDialogs` (минимальный список для селекта), либо отдельный компактный `useDialogSelector`.
  - `useTopics(getApiKey)` — уже есть в dialogs-messages (принимает dialogId), можно вынести в общий модуль или скопировать логику для topics-messages.
  - `useMessagesByTopic(getApiKey)` — то же, что и для A: сообщения по `dialogId` + `topicId`.

### Общее с существующими страницами

- Переиспользовать: BasePanel, пагинация, стили таблиц, формат фильтров (где применимо).
- Таблица сообщений — та же по смыслу, что на «Диалоги + Сообщения» (MessagesTableSimple), с источником данных из `useMessagesByTopic`.

---

## Краткий порядок работ (после ответов на вопросы)

1. Зафиксировать вариант (A или B) и ответы на вопросы 2–5.
2. **Вариант A:** спроектировать и добавить `GET /api/topics` в tenant-api (контроллер, роут, валидация, пагинация, фильтр по dialogId/topicId/meta при необходимости).
3. Роутер: добавить маршрут `/topics-messages`.
4. Sidebar: добавить пункт «Топики + Сообщения».
5. Композейблы: `useTopicsList` (A) или использование выбора диалога + `useTopics` (B), `useMessagesByTopic`.
6. UI: страница TopicsMessagesPage.vue, две колонки, таблицы топиков и сообщений, фильтры, пагинация.
7. Проверка сценариев: выбор топика → загрузка сообщений, пагинация, фильтр сообщений.

Когда определишься с вариантом и ответами на вопросы — можно детализировать план под выбранный вариант (в т.ч. точные сигнатуры API и composables).
